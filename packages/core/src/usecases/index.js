// @ts-check

/**
 * Use Cases - Business logic and application workflows.
 * 
 * Use cases are implemented as factory functions that accept dependencies (ports)
 * and return configured functions that execute the business logic.
 */

export { makeRunSearch } from './runSearch.js';
